import { Hero } from "@/components/home-components/hero-components/hero";
import React from "react";

const Home = () => {
  return (
    <section>
      <Hero />
    </section>
  );
};

export default Home;
